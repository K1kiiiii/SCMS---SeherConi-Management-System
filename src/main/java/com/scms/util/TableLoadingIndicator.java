package com.scms.util;

/**
 * Placeholder removed. Use LoadingOverlay.show(node) / LoadingOverlay.hide(node) instead.
 */
public final class TableLoadingIndicator {
    private TableLoadingIndicator() {}
}
